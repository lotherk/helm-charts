apiVersion: v1
kind: Service
metadata:
  name: {{ include "fiche.fullname" . }}-tcp
  labels:
    {{- include "fiche.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.fiche.service.annotations | nindent 4 }}
spec:
  type: {{ .Values.fiche.service.type }}
  {{- if eq .Values.fiche.service.type "LoadBalancer" }}
  {{- if .Values.fiche.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.fiche.service.loadBalancerIP }}
  {{- end -}}
  {{- if .Values.fiche.service.loadBalancerSourceRanges }}
  loadBalancerSourceRanges:
  {{- range .Values.fiche.service.loadBalancerSourceRanges }}
    - {{ . }}
  {{- end }}
  {{- end }}
  {{- end }}
  {{- if and .Values.fiche.service.clusterIP (eq .Values.fiche.service.type "ClusterIP") }}
  clusterIP: {{ .Values.fiche.service.clusterIP }}
  {{- end }}
  {{- if .Values.fiche.service.externalIPs }}
  externalIPs:
    {{- toYaml .Values.fiche.service.externalIPs | nindent 4 }}
  {{- end }}
  {{- if .Values.fiche.service.externalTrafficPolicy }}
  externalTrafficPolicy: {{ .Values.fiche.service.externalTrafficPolicy }}
  {{- end }}
  ports:
  - name: {{ include "fiche.fullname" . }}-tcp
    port: {{ .Values.fiche.service.port }}
    targetPort: 9999
    protocol: TCP
    {{- if .Values.fiche.service.nodePort }}
    nodePort: {{ .Values.fiche.service.nodePort }}
    {{- end }}

  selector:
    {{- include "fiche.selectorLabels" . | nindent 4 }}
