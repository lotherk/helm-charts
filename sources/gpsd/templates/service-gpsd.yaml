apiVersion: v1
kind: Service
metadata:
  name: {{ include "gpsd.fullname" . }}-tcp
  labels:
    {{- include "gpsd.labels" . | nindent 4 }}
    app.kubernetes.io/component: gpsd
  annotations:
    {{- toYaml .Values.gpsd.service.annotations | nindent 4 }}
spec:
  type: {{ .Values.gpsd.service.type }}
  {{- if eq .Values.gpsd.service.type "LoadBalancer" }}
  {{- if .Values.gpsd.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.gpsd.service.loadBalancerIP }}
  {{- end -}}
  {{- if .Values.gpsd.service.loadBalancerSourceRanges }}
  loadBalancerSourceRanges:
  {{- range .Values.gpsd.service.loadBalancerSourceRanges }}
    - {{ . }}
  {{- end }}
  {{- end }}
  {{- end }}
  {{- if and .Values.gpsd.service.clusterIP (eq .Values.gpsd.service.type "ClusterIP") }}
  clusterIP: {{ .Values.gpsd.service.clusterIP }}
  {{- end }}
  {{- if .Values.gpsd.service.externalIPs }}
  externalIPs:
    {{- toYaml .Values.gpsd.service.externalIPs | nindent 4 }}
  {{- end }}
  {{- if .Values.gpsd.service.externalTrafficPolicy }}
  externalTrafficPolicy: {{ .Values.gpsd.service.externalTrafficPolicy }}
  {{- end }}
  ports:
  - name: {{ include "gpsd.fullname" . }}-tcp
    port: {{ .Values.gpsd.service.port }}
    targetPort: 2947
    protocol: TCP
    {{- if .Values.gpsd.service.nodePort }}
    nodePort: {{ .Values.gpsd.service.nodePort }}
    {{- end }}

  selector:
    {{- include "gpsd.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: gpsd
